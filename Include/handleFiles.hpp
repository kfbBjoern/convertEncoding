#ifndef HANDLE_FILES
#define HANDLE_FILES

#include <string>
#include <vector>


void  addFiles(std::vector< std::string >& files, std::string parameter);

bool convertFile(std::string& path);

#endif // HANDLE_FILES