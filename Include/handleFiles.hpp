#ifndef HANDLE_FILES
#define HANDLE_FILES

#include <string>
#include <vector>


void  addFiles(std::vector< std::string >& files, std::string parameter);

void convertFile(std::string& path);

#endif // HANDLE_FILES